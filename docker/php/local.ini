memory_limit = 512M      ; Prevents Composer and heavy queries from crashing
upload_max_filesize = 64M ; Allows larger file/image uploads via forms
post_max_size = 64M       ; Must be equal to or larger than upload_max_filesize
max_execution_time = 120  ; Prevents long-running scripts from timing out
